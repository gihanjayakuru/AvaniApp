import 'package:flutter/material.dart';

class FormDataListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Form Data List'),
      ),
      body: Container(
        child: Text('Display the list of form data here'),
      ),
    );
  }
}
